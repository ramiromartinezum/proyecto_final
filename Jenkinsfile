// Jenkinsfile
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Instalar dependencias si es necesario
                // Ejecutar el script principal
                sh 'python3 main.py'
            }
        }
    }
}
